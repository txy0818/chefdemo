server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/chef_reservation_system?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 12345678
  redis:
    host: localhost
    port: 6379
    database: 0

mybatis:
  mapper-locations: classpath:mapper/*.xml

minio:
  endpoint: http://127.0.0.1:9000
  public-endpoint: http://127.0.0.1:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: chef-platform
  upload-expiry-seconds: 900
  auto-create-bucket: true
#logging:
#  file:
#    name: logs/plugin-system.log
#  level:
#    root: info
